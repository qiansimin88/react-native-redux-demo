import * as types from '../action/actionCollection'

//todo的reducer


const todo = ( state = {}, action ) => {
    switch (action.type) {
        case types.ADD_TODO: 
            return {}
    }
}

export default todo;  